<%= form_for(@year_class) do |f| %>
  <% if @year_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@year_class.errors.count, "error") %> prohibited this year_class from being saved:</h2>

      <ul>
      <% @year_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
