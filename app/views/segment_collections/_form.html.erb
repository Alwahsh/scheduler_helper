<%= form_for(@segment_collection) do |f| %>
  <% if @segment_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@segment_collection.errors.count, "error") %> prohibited this segment_collection from being saved:</h2>

      <ul>
      <% @segment_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
